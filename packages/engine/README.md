# packages/engine

Deterministic decision engine — pure TypeScript, zero runtime dependencies.

`decide(snapshot: EnergySnapshot): RecommendationResult`

- No I/O, no side effects, fully unit-testable
- 100% coverage required by CI
- Implemented in **Session 2**. See [`SESSIONS.md`](../../SESSIONS.md).
